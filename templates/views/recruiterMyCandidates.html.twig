



{#<h1>my candidates</h1>#}
{#<ul class="candidate-list">#}
{#        {% for element in candidates %}#}
{#                <div>#}
{#                    {{ element.id }}#}
{#                    {{ element.firstName }}#}
{#                    {{ element.lastName }}#}
{#                    {{ element.email }}#}
{#                    {% if element.resume != null %}#}
{#                        {{ element.resume }}#}
{#                    {% endif %}#}



{#                    <a href="#">#}
{#                        <button>#}
{#                            Send Him Email#}
{#                        </button>#}
{#                    </a>#}

{#                    <a href="#">#}
{#                        <button>#}
{#                            See Resume#}
{#                        </button>#}
{#                    </a>#}
{#                </div>#}
{#        {% endfor %}#}
{#</ul>#}


<!DOCTYPE html>
<html>
<head>
    <title>My Candidates</title>
    <style>
        body {
            background-color: #F7F7F7;
            font-family: Arial, sans-serif;
            color: #444;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #FFF;
            border-radius: 5px;
            box-shadow: 0px 0px 10px rgba(0,0,0,0.1);
        }

        h1 {
            font-size: 36px;
            font-weight: bold;
            text-align: center;
            margin-top: 0;
            margin-bottom: 20px;
        }

        .candidate-list {
            list-style-type: none;
            padding: 0;
        }

        .candidate-list div {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background-color: #EEE;
            padding: 10px;
            margin-bottom: 10px;
            border-radius: 5px;
            box-shadow: 0px 0px 5px rgba(0,0,0,0.1);
        }

        .candidate-list div:hover {
            box-shadow: 0px 0px 10px rgba(0,0,0,0.2);
        }

        .candidate-details {
            flex: 1;
            margin-right: 10px;
        }

        .candidate-details h2 {
            font-size: 24px;
            margin-top: 0;
            margin-bottom: 10px;
        }

        .candidate-details p {
            margin: 0;
        }

        .candidate-actions {
            display: flex;
            align-items: center;
        }

        .candidate-actions button {
            background-color: #4285F4;
            color: #FFF;
            border: none;
            padding: 10px 20px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 16px;
            margin-right: 10px;
            cursor: pointer;
            border-radius: 5px;
            transition: all 0.2s ease-in-out;
        }

        .candidate-actions button:hover {
            background-color: #0D47A1;
        }
    </style>
</head>
<body>
<div class="container">
    <h1>My Candidates</h1>
    <ul class="candidate-list">
        {% for element in candidates %}
            <div>
                <div class="candidate-details">
                    <h2>{{ element.firstName }} {{ element.lastName }}</h2>
                    <p>{{ element.email }}</p>
                </div>
                <div class="candidate-actions">
                    <a href="mailto:{{ element.email }}">
                        <button>Send Email</button>
                    </a>
                    {% if element.resume is defined %}
                        <a href="{{ path('app_recruiters_displayresume', { 'id': recruiterId, 'candidateId': element.id}) }}">
                            <button>View Resume</button>
                        </a>
                    {% endif %}
                </div>
            </div>
        {% endfor %}
    </ul>
</div>
</body>
</html>